cmake_minimum_required(VERSION 3.13)

project({{Global::project_name}}-ecs)
set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS}")
set(CMAKE_STATIC_LINKER_FLAGS "${CMAKE_STATIC_LINKER_FLAGS}")
set(CMAKE_CXX_STANDARD 20)

add_library({{Global::project_name}}-ecs STATIC
            loader.cpp
            lua_ecs.cpp
            scene.cpp)

target_link_libraries({{Global::project_name}}-ecs PRIVATE Sprocket)
target_include_directories({{Global::project_name}}-ecs PUBLIC .)