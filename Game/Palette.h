#pragma once
#include <Sprocket.h>

// Indian Palette (flatuicolors.com)
const auto ORCHID      = spkt::FromHex(0xFEA47F);
const auto GARDENIA    = spkt::FromHex(0xF97F51);
const auto FUCHSIA     = spkt::FromHex(0xB33771);
const auto MAGENTA     = spkt::FromHex(0x6D214F);

const auto LIGHT_BLUE  = spkt::FromHex(0x25CCF7);
const auto CLEAR_BLUE  = spkt::FromHex(0x1B9CFC);
const auto MIDNIGHT    = spkt::FromHex(0x3B3B98);
const auto NAVY_NIGHT  = spkt::FromHex(0x182C61);

const auto HONEY       = spkt::FromHex(0xEAB543);
const auto SARAWAK     = spkt::FromHex(0xF8EFBA);
const auto PEACH       = spkt::FromHex(0xFD7272);
const auto PINK        = spkt::FromHex(0xFC427B);

const auto GARDEN      = spkt::FromHex(0x55E6C1);
const auto KEPPEL      = spkt::FromHex(0x58B19F);
const auto OASIS       = spkt::FromHex(0x9AECDB);
const auto PINE_GLADE  = spkt::FromHex(0xBDC581);

const auto STAR_WHITE  = spkt::FromHex(0xCAD3C8);
const auto SPACE_DARK  = spkt::FromHex(0x2C3A47);
const auto PALE_INDIGO = spkt::FromHex(0x9AECDB);
const auto LAVENDER    = spkt::FromHex(0xBDC581);